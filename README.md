# 📝 Formulário de Cadastro de Aluno

Este projeto simples consiste em um formulário web responsivo para o cadastro de informações de alunos, utilizando as tecnologias essenciais do desenvolvimento web: HTML, CSS, Bootstrap e JavaScript.

O formulário inclui validação básica de campos e demonstra como coletar e formatar os dados inseridos, especificamente ajustando o formato de datas para o padrão brasileiro (DD-MM-YYYY).

## 🚀 Tecnologias Utilizadas

* *HTML5:* Estrutura base do formulário.
* *CSS3:* Estilização básica e customizações de cores.
* *Bootstrap 5:* Framework para layout responsivo, componentes e cores padrão (inputs e botões).
* *JavaScript (Vanilla JS):* Lógica para validação de formulário (junto com Bootstrap) e formatação/coleta dos dados para exibição.

## ✨ Funcionalidades

* *Validação de Campos:* Utiliza a validação nativa do Bootstrap/HTML5 para campos obrigatórios.
* *Responsividade:* O layout se adapta bem a diferentes tamanhos de tela (desktop e mobile).
* *Customização de Estilo:* Cor do focus ring dos inputs alterada de azul para *verde*.
* *Formatação de Datas:* As datas coletadas (YYYY-MM-DD) são automaticamente convertidas para o formato brasileiro (DD-MM-YYYY) antes de serem exibidas no JSON de saída.
* *Simulação de Submissão:* Os dados são coletados e exibidos na tela em formato JSON, simulando o que seria enviado a um backend.
